{
  "name": "driftline-monorepo",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "playground"
  ],
  "scripts": {
    "dev": "npm run dev -w playground",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint packages playground --ext .ts,.tsx"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/er-Bilim/driftline.git"
  }
}
